## Тема: Списки и словари в Python

### Цель урока: 

Познакомиться со списками и словарями в яп python

# Этап 1: работа со списками

### Шаг 1:

Во многих задачах требуется **сохранять всю последовательность**. Например, классическая задача сортировки некоторой последовательности требует сохранения всех данных в памяти компьютера. Увы, не сохранив, их невозможно отсортировать. И тут на помощь приходит структура данных, которая в большинстве языков программирования называется массивом. В Python она называется **списком**.

Список представляет собой последовательность элементов, пронумерованных от 0, как символы в строке.

Чтобы создать список, нужно перечислить его элементы через запятую в квадратных скобках:

```python
numbers = [2, 4, 6, 8, 10]
languages = ['Python', 'C#', 'C++', 'Java']
```

Список `numbers` состоит из 5 элементов, и каждый из них — целое число.

- numbers[0] == 2;
- numbers[1] == 4;
- numbers[2] == 6;
- numbers[3] == 8;
- numbers[4] == 10.

Список `languages` состоит из 4 элементов, каждый из которых — **строка.**

- languages[0] == 'Python';
- languages[1] == 'C#';
- languages[2] == 'C++';
- languages[3] == 'Java'.

Список может содержать значения **разных типов данных**:

```
info = ['Python', 1987, 52.5]
```

Список `info`:

- info[0] = 'Python';
- info[1] = 1987;
- info[2] = 52.5

#### Вывод списка

Для вывода всего списка можно применить функцию `print()`:

```python
numbers = [2, 4, 6, 8, 10]
languages = ['Python', 'C#', 'C++', 'Java']
print(numbers)
print(languages)
```

Функция `print()` выводит на экран элементы списка, в квадратных скобках, разделенные запятыми:

```python
[2, 4, 6, 8, 10]
['Python', 'C#', 'C++', 'Java']
```


### Шаг 2

Из скольких элементов состоит список `numbers`?

```python
numbers = [3, 5, 7, 9]
```

- 5 
- 4 (правильный ответ)
- 3
- 7

Какой индекс у числа 1 в списке `numbers`?

```python
numbers = [1, 100, 7, 20, 17, 37, 22]
```  

- 0 (правильный ответ)
- 5
- 1
- 2

Какой индекс у числа 17 в списке `numbers`?

```python
numbers = [1, 100, 7, 20, 17, 37, 22]
```  

- 8
- 5
- 4 (правильный ответ)
- 2

Что покажет приведённый ниже код?

```python
numbers = [3, 1, 4, 1, 5]

print(numbers[1] == 3)
print(numbers[4] == 5)
```

- True False
- True True
- False False
- False True
- ошибка

### Шаг 3 пример кода

На вход программе подаётся число n(n≥1)n(n≥1). Нужно написать программу, которая создаёт список из нечётных чисел от 1 до nn (включительно) и выводит его.

| тест | входные данные | выходные данные |
| ---- | -------------- | --------------- |
| 1    | 5              | [1, 3, 5]       |
| 2    | 4              | [1, 3]          |
| 3    | 8              | [1, 3, 5, 7]    |

Для начала нам надо понимать как создавать массив

```python
arr = [8, 10, 77] # название_переменной = []
```

Причем, в питоне не обязательно указывать длину массива, как в других языках программирования, например, в c#, c++ и других это обязательно.

#### Решение задачи:

```
# перехватываем ввод пользователя
n = int(input()) 

# объявляем пустой массив
arr = []

# создаем цикл от 1 до n включительно
for i in range (1, n + 1):
	
	# если переменная в цикле нечетная => кидаем в массив
    if (i % 2 != 0):
        arr.append(i)
        
# вывод массива по требованию выходных данных
print(arr)
```

Но у решения этой задачи (как и у многих других) есть альтернативы:

1:
```
n = int(input())
arr = []

for i in range (1, n + 1, 2): # не используем условие, а шаг сразу через 2
    arr.append(i)
        
print(arr)
```

2:
```
n = int(input())
arr = list(range(1, n + 1, 2))

print(arr)
```

А что за list ?

Python имеет встроенную функцию `list()`, которая помимо создания пустого списка может преобразовывать некоторые типы объектов в списки.

Например, мы знаем, что функция `range()` создает последовательность целых чисел в заданном диапазоне. Для преобразования этой последовательности в список, мы пишем следующий код:

```python
numbers = list(range(5))
```
Во время исполнения этого кода происходит следующее:

1. Вызывается функция `range()`, в которую в качестве аргумента передается число 5;
2. Эта функция возвращает последовательность чисел `0, 1, 2, 3, 4` ;
3. Последовательность чисел `0, 1, 2, 3, 4` передается в качестве аргумента в функцию `list()`;
4. Функция `list()` возвращает список `[0, 1, 2, 3, 4]`;
5. Список `[0, 1, 2, 3, 4]` присваивается переменной `numbers`.

Точно так же с помощью функции `list()` мы можем создать список из символов строки. Для преобразования строки в список мы пишем следующий код:

```python
s = 'abcde'
chars = list(s) # список содержит символы 'a', 'b', 'c', 'd', 'e'
```

---
#### Функция len()

**Длиной списка** называется количество его элементов. Для того чтобы посчитать длину списка, мы используем встроенную функцию `len()` (от слова length – длина).

```python

languages = ['Python', 'C#', 'C++', 'Java']

print(len(numbers))      # выводим длину списка numbers
print(len(languages))    # выводим длину списка languages

print(len(['apple', 'banana', 'cherry']))   # выводим длину списка, состоящего из 3 элементов
```

выводит:

```
5
4
3
```

### Функции min и max

Для того чтобы подсчитать минимальный и максимальный элемент в массиве используются соотвественно функции min и max.

```python
numbers = [121, 123, 123, 127, 125, 124]

print(min(numbers)) # 121
print(max(numbers)) # 127
```


#### Индексация

Чтобы изменить элемент массива с индексом 2 мы можем использовать такую конструкцию:

```python
numbers = [2, 4, 6, 8, 10]
numbers[2] = 47

print(numbers) # [2, 4, 47, 8, 10]
```

#### Метод append()

Для добавления нового элемента **в конец списка** используется метод `append()`.

Приведённый ниже код:

```python
numbers = [1, 1, 2, 3, 5, 8, 13]  # создаем список

numbers.append(21)  # добавляем число 21 в конец списка
numbers.append(34)  # добавляем число 34 в конец списка

print(numbers)
```

 выводит:

```no-highlight
[1, 1, 2, 3, 5, 8, 13, 21, 34]
```

**Примечание.** 
Чтобы узнать сумму всех чисел в списке, используют функцию `sum()`. Например: `sum([1, 2, 3])` вернет `6`.

чтобы получить самый последний элемент списка, можно использовать его длину: `numbers[len(numbers) - 1]`.


### Задачка

На вход программе подаются натуральное число n, а затем n строк. Напишите программу, которая создаёт из указанных строк список, а затем выводит его.

**Формат входных данных**  
На вход программе подаются натуральное число n, а затем n строк, каждая на отдельной строке.

**Формат выходных данных**  
Программа должна вывести список, состоящий из указанных строк.

| тест | входные данные                          | выходные данные                        |
| ---- | --------------------------------------- | -------------------------------------- |
| 1    | 5<br>C#<br>C++<br>C<br>Python<br>Golang | ['C#', 'C++', 'C', 'Python', 'Golang'] |

```python
# Перехватываем число ввода пользователя (n строк)
n = int(input())

# Создаем пустой массив
arr = []

# от 0 до n
for i in range(n):

	# Перехватываем ввод строки пользователем
    s = input()
    
    # Добавим строку в масси
    arr.append(s)
    
# Вывод по требованию в задании
print(arr)
```


# Этап 2: работа с словарями

**Cловари** – изменяемые коллекции элементов **с произвольными индексами – ключами**. Если в списках элементы индексируются целыми числами, начиная с `0`, то в словарях — любыми ключами, в том числе в виде строк.

Допустим, мы хотим хранить имя создателя каждого языка программирования. Это можно сделать несколькими способами.

**Способ 1.** Хранить еще один список, где по соответствующему индексу будет находиться имя создателя языка программирования.

Приведенный ниже код:

```python
languages = ['Python', 'C#', 'Java', 'C++']
creators = ['Гвидо ван Россум', 'Андерс Хейлсберг', 'Джеймс Гослинг', 'Бьёрн Страуструп']

print('Создателем языка', languages[0], 'является', creators[0])
```

выводит:

```
Создателем языка Python является Гвидо ван Россум
```

### Создание словаря

Чтобы создать словарь, нужно перечислить его элементы – пары ключ-значение – через запятую в фигурных скобках, как и элементы множества. Первым указывается ключ, после двоеточия — значение, доступное в словаре по этому ключу.

Приведенный ниже код:

```python
languages = {
	'Python': 'Гвидо ван Россум', 
	'C#': 'Андерс Хейлсберг', 
	'Java': 'Джеймс Гослинг', 
	'C++': 'Бьёрн Страуструп'
}
```

создает словарь, в котором ключом служит строка — название языка программирования, а значением — имя создателя языка.

```python
print('Создателем языка C# является', languages['C#'])
```

выводит:
```no-highlight
Создателем языка C# является Андерс Хейлсберг
```

В отличие от списков, номеров позиций в словарях нет.

**Примечание.** Словари удобно использовать для хранения различных сущностей. Например, если нужно работать с информацией о человеке, то можно хранить все необходимые сведения, включающие такие разные сущности как "имя", "возвраст", "оценки которые он получил", "адрес электронной почты" в одном словаре  `info` и легко обращаться к его элементам по ключам:

```python
info = {
	'name': 'Ivan',
	'surname': 'Ivanov',
	'grades': [5, 4, 5, 3, 5, 5],
	'email': 'ivanivanov@yandex.com'
}

print(info['name'], info['surname']) # Ivan Ivanov
print(info['grades']) # [5, 4, 5, 3, 5, 5]
```

### Шаг 2

Что покажет приведенный ниже код?

```python
stuff = {1: 'ааа', 2: 'ббб', 3: 'ввв'}
print(stuff[2])
```

- 1
- 2
- 3
- ааа
- ббб (правильный ответ)
- ввв

Что покажет приведенный ниже код?

```python
my_dict = {1: [0, 1], 2: [2, 3], 3: [4, 5]}
print(my_dict[2][1])
```

- [2, 3]
- [4, 5]
- 2
- 3 (правильный ответ)
- [0, 1]
- 1

### Шаг 3

| **Номер курса (ключ)** | Номер аудитории (значение) | Преподаватель (значение) | Время (значение) |
| ---------------------- | -------------------------- | ------------------------ | ---------------- |
| CS101                  | 3004                       | Хайнс                    | 8:00             |
| CS102                  | 4501                       | Альварадо                | 9:00             |
| CS103                  | 6755                       | Рич                      | 10:00            |
| NT110                  | 1244                       | Берк                     | 11:00            |
| CM241                  | 1411                       | Ли                       | 13:00            |

**Формат входных данных**  
На вход программе подается одна строка – номер курса.

**Формат выходных данных**  
Программа должна вывести номер курса, затем номер аудитории, имя преподавателя и время проведения курса в соответствии с примерами.

**Примечание.** Используйте словарь вместо условного оператора.

| Номер теста | Входные данные | Выходные данные              |
| ----------- | -------------- | ---------------------------- |
| 1           | СS101          | CS101: 3004, Хайнс, 8:00     |
| 2           | CM241          | CM241: 1411, Ли, 13:00       |
| 3           | CS102          | CS102: 4501, Альварадо, 9:00 |

```python
# объявляем словарь для каждого класса
courses = {  
    'CS101': {  
        'classroom': 3004, 'teacher': 'Хайнс', 'time': '8:00'  
    },  
    'CS102': {  
        'classroom': 4501, 'teacher': 'Альварадо', 'time': '9:00'  
    },  
    'CS103': {  
        'classroom': 6755, 'teacher': 'Рич', 'time': '10:00'  
    },  
    'NT110': {  
        'classroom': 1244, 'teacher': 'Берк', 'time': '11:00'  
    },  
    'CM241': {  
        'classroom': 1411, 'teacher': 'Ли', 'time': '13:00'  
    },  
}

n = input()

# Добавляем проверку, есть ли такой курс в словаре, чтобы программа не падала
print(f"{n}: {courses[n]['classroom']}, {courses[n]['teacher']}, {courses[n]['time']}")
```

## Этап 3: Комплексное практическое задание

### Задание «Электронный журнал»

**Контекст:** Вы разрабатываете модуль для школьного электронного журнала. В программе уже хранится база данных учеников с их оценками по программированию.

**Формат входных данных:** На вход программе подается одно целое число — **пороговый балл** (от 2 до 5).

**Во время работы программа должна:**

1. Посчитать и вывести **средний балл** первого ученика в списке (Ивана).
    
2. Найти и вывести **минимальную** и **максимальную** оценку второго ученика (Анны).
    
3. Используя цикл `for` и условие `if`, найти и вывести имена тех учеников, у которых **последняя оценка** в списке равна или выше порогового балла, введенного пользователем.

```
# База данных учеников (список, содержащий словари)
students = [
    {
        'name': 'Иван',
        'grades': [5, 4, 5, 3, 5]
    },
    {
        'name': 'Анна',
        'grades': [4, 5, 4, 5, 4]
    },
    {
        'name': 'Петр',
        'grades': [3, 3, 4, 2, 3]
    },
    {
        'name': 'Дарья',
        'grades': [5, 5, 5, 4, 5]
    }
]

# 1. Перехватываем ввод пользователя (пороговый балл)
limit = int(input("Введите пороговый балл: "))

# --- ВАШ КОД НАЧИНАЕТСЯ ТУТ ---

# Шаг 1: Считаем средний балл Ивана (индекс 0 в списке students)
# Подсказка: сумма элементов делится на их количество len()
ivan_grades = students[0]['grades']
ivan_avg = sum(ivan_grades) / len(ivan_grades)
print(f"Средний балл Ивана: {ivan_avg}")

# Шаг 2: Находим min и max оценки Анны (индекс 1 в списке students)
anna_grades = students[1]['grades']
print(f"Оценки Анны - Мин: {min(anna_grades)}, Макс: {max(anna_grades)}")

# Шаг 3: Цикл и условие. Выводим тех, у кого ПОСЛЕДНЯЯ оценка >= limit
print(f"Ученики с последней оценкой не ниже {limit}:")

for student in students:
    # Получаем список оценок текущего ученика
    grades = student['grades'] 
    
    # Индекс последнего элемента можно найти через len(grades) - 1
    last_grade = grades[len(grades) - 1] 
    
    # Проверяем условие
    if last_grade >= limit:
        print(student['name'])
```

### Примеры тестов для проверки

#### Тест 1

- **Входные данные:** `5`

- **Выходные данные:**

```
Средний балл Ивана: 4.4
Оценки Анны - Мин: 4, Макс: 5
Ученики с последней оценкой не ниже 5:
Иван
Дарья
```

#### Тест 2

- **Входные данные:** `3`

- **Выходные данные:**

```
Средний балл Ивана: 4.4
Оценки Анны - Мин: 4, Макс: 5
Ученики с последней оценкой не ниже 3:
Иван
Анна
Петр
Дарья
```
